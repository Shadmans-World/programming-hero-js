const colors = ['red', 'blue', 'green', 'yellow', 'orange'];

var rev = [];
for(var i = colors.length-1; i>=0; i--){
    num = colors[i];
    
    console.log(num);
    rev.push(num)
}
console.log(rev)

